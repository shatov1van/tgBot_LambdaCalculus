meta:
  environment: ruby
  toolchain:
    name: bundle
    version: 3.4
build:
  image: null
run:
  image: null
  mainScript: "sh -c 'bundle install && bundle exec ruby bot.rb'"
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
