meta:
  environment: ruby
  toolchain:
    name: bundle
    version: 3.4
build:
  image: null
run:
  image: null
  mainScript: "bundle exec ruby bot.rb"
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
